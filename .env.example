# =============================================================================
# Online Code Editor - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# The editor fetches student_id, topic_id, course_id, level_id from URL params
# (passed by the LMS frontend when embedding the editor).
# Save/load uses the backend API, which connects to Supabase.
# =============================================================================

# Backend API URL - where save/load requests go (student_saved_projects table)
# The backend uses its own .env with SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY.
# When embedded, apiBase is passed in the iframe URL; this is the fallback.
REACT_APP_API_BASE_URL=http://localhost:3001

# URL params the editor expects (passed by LMS when opening editor):
# - studentId, topicId, courseId, levelId (required - from URL)
# - apiBase, authToken (for API-based save/load)
# - savedProjectId or projectId (when loading existing project)
# - topicName, levelName, courseName (optional, for display)
